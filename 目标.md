以下是一份经过精心设计的完整提示词，您可以直接交给 AI 使用：

---

## 提示词（可直接复制使用）

---

**项目名称：** ML-Visualizer —— 机器学习与深度学习交互式知识可视化平台

**角色设定：**
你是一位兼具顶尖前端工程师、数据可视化专家与机器学习研究者三重身份的资深开发者。你需要构建一个面向研究生水平的机器学习知识可视化 Web 平台，追求极致的视觉品质、动画流畅性与内容深度。

---

### 一、技术栈要求

- **核心框架：** Vue 3 + Vite（或 React 18 + Vite，选择你最擅长的）
- **动画与可视化：** Three.js（3D神经网络渲染）、D3.js（数据图表与流程图）、GSAP（补间动画）、Canvas API / WebGL（自定义动画）
- **UI 框架：** Tailwind CSS + 自定义 CSS 变量（深色主题为主，支持亮色切换）
- **代码演示：** Monaco Editor（内嵌代码编辑器，支持 Python 语法高亮与伪执行说明）
- **数学公式：** KaTeX 或 MathJax（渲染 LaTeX 公式）
- **图表库：** Chart.js 或 ECharts（损失曲线、准确率曲线等动态图表）
- **路由：** Vue Router 4（多页面 SPA 导航）
- **兼容性：** 全面适配 Microsoft Edge（Chromium 内核），CSS 使用标准写法，避免非标准前缀，测试分辨率覆盖 1366×768 至 2560×1440
- **语言：** 界面语言为简体中文，代码注释为中英双语

---

### 二、视觉设计规范

**整体风格：** 科技感深色主题，主色调为深蓝（#0A0E1A）+ 电光蓝（#00D4FF）+ 紫罗兰（#7B61FF），强调色为青绿（#00FFB3）。字体使用 Inter（英文）+ 思源黑体（中文）。

**设计细节：**
- 毛玻璃（Glassmorphism）卡片效果：`backdrop-filter: blur(12px)`，边框使用半透明白色
- 所有卡片、模块悬浮时有柔和的发光阴影（box-shadow 使用主题色）
- 页面滚动时触发动画（Intersection Observer），知识点在进入视口时以淡入+上移方式出现
- 顶部导航栏固定，含毛玻璃效果，滚动后自动收缩
- 粒子背景（使用 Canvas 或 Three.js），粒子稀疏、轻柔，不干扰阅读
- 响应式布局，侧边栏在宽屏展开，移动端收起为汉堡菜单

---

### 三、核心可视化动画（最高优先级）

以下动画必须实现，且必须流畅（目标 60fps）：

**① 神经网络信息流动动画（核心亮点）**
用 Canvas 或 SVG 绘制多层神经网络图（可配置层数与每层节点数）。信息正向传播时，激活信号从输入层向输出层"流动"：信号到达某个节点，该节点立刻发光变亮（使用径向渐变 + 动态透明度），连接线上有移动的光点（沿贝塞尔曲线运动的粒子），颜色从冷色（未激活，深蓝灰）渐变到暖色（高激活，橙黄或青绿）。支持反向传播模式：梯度从输出层向输入层回流，颜色变为紫色系，动画方向反转。提供"播放/暂停/重置/调速"控制栏。

**② 梯度下降可视化**
用 Three.js 渲染一个三维损失函数曲面（如双碗形或鞍点形），一个小球代表参数点，在曲面上按梯度方向滚动下降。支持切换：SGD、Momentum、Adam、RMSProp 等优化器，不同优化器小球轨迹用不同颜色叠加显示。支持鼠标拖拽旋转视角，滚轮缩放。

**③ 卷积操作动画**
用 Canvas 动态演示卷积核在输入特征图上滑动的过程：卷积核高亮当前覆盖区域，右侧实时显示输出特征图逐步填充的过程，配合数字跳动动画。支持用户修改卷积核数值，实时重算并演示。

**④ 注意力机制（Attention）热力图动画**
展示 Transformer Self-Attention：输入 token 序列横排显示，注意力权重矩阵以热力图形式动态渲染（颜色从冷到热），鼠标悬停某个 token 时，与其他 token 的注意力连线动态高亮，线条粗细代表权重大小。

**⑤ 激活函数交互图**
用 D3.js 实时绘制 ReLU、Sigmoid、Tanh、GELU、Swish 等激活函数曲线。鼠标滑动时显示对应点的函数值与导数值，支持多函数叠加对比显示，动画过渡切换。

**⑥ 训练过程动态仪表盘**
模拟训练过程：损失曲线与准确率曲线实时"增长"（用 Chart.js 动态 push 数据），支持训练集/验证集双线对比，自动标注过拟合区域（验证损失开始上升时用红色阴影覆盖）。

---

### 四、知识内容结构（详细且深刻）

请按以下结构组织内容，每个知识点包含：**原理讲解（通俗+数学）→ 可视化演示 → 代码实现（Python/PyTorch/TensorFlow）→ 关键论文导读 → 论文写作指导**。

#### 模块 1：机器学习基础
- 监督/无监督/强化学习的本质区别（用动画展示决策边界的形成过程）
- 偏差-方差权衡（Bias-Variance Tradeoff）：动态展示拟合曲线随模型复杂度变化
- 损失函数全景：MSE、Cross-Entropy、Focal Loss、Contrastive Loss，含公式与适用场景
- 正则化：L1（稀疏性动画）、L2（权重收缩动画）、Dropout（节点随机消失动画）
- 特征工程与数据预处理：归一化、标准化的效果对比动画
- **论文写作指导：** 如何在论文中规范描述实验设置、基线选择与评估指标

#### 模块 2：深度学习核心
- 反向传播算法：链式法则的逐步计算动画（计算图节点逐步标注梯度值）
- 优化器深度解析：从 SGD 到 Adam，数学推导 + 三维曲面轨迹对比动画
- 批归一化（Batch Normalization）：分布变化动画，解释为何加速训练
- 学习率调度：Warmup + Cosine Decay 曲线动画，解释其直觉
- 权重初始化：Xavier、Kaiming 初始化，信号传播方差分析动画
- **论文写作指导：** 消融实验（Ablation Study）设计规范，如何证明每个组件的有效性

#### 模块 3：卷积神经网络（CNN）
- 卷积、池化、步长、填充的完整动画演示
- 经典架构演进：LeNet → AlexNet → VGG → ResNet → EfficientNet（时间轴动画）
- 残差连接（Skip Connection）：梯度流动对比动画（有/无残差）
- 感受野（Receptive Field）计算可视化
- 迁移学习：特征层冻结策略动画
- **论文写作指导：** 如何撰写网络架构章节，绘制规范的架构图

#### 模块 4：循环神经网络与序列模型
- RNN 梯度消失/爆炸：动画展示梯度随时间步的衰减
- LSTM 门控机制：细胞状态的流动动画（遗忘门、输入门、输出门逐步激活）
- GRU 简化结构对比动画
- Seq2Seq + Attention 机制演示
- **论文写作指导：** 序列任务的评估指标（BLEU、ROUGE）解释与引用规范

#### 模块 5：Transformer 与大模型
- Self-Attention 完整计算过程动画（Q、K、V 矩阵乘法步骤分解）
- 多头注意力（Multi-Head Attention）：多个注意力头并行动画
- 位置编码（Positional Encoding）：正弦波可视化
- BERT、GPT 架构对比，预训练-微调范式动画
- ViT（Vision Transformer）：图像 Patch 分割动画
- **论文写作指导：** 如何在顶会论文中描述 Transformer 变体，常见创新点的写法

#### 模块 6：生成模型
- GAN 训练博弈动画：生成器与判别器损失曲线的对抗演变
- VAE：隐空间插值动画（二维隐空间中的点游走，对应图像变化）
- 扩散模型（Diffusion Model）：加噪与去噪过程的逐步动画
- **论文写作指导：** 生成模型评估指标（FID、IS）的正确引用方式

#### 模块 7：实践与工程
- 完整项目流程：数据收集 → 预处理 → 建模 → 训练 → 评估 → 部署（流程图动画）
- 超参数调优：网格搜索、随机搜索、贝叶斯优化的搜索过程动画
- 模型可解释性：Grad-CAM 热力图演示，SHAP 值条形图动画
- 常用工具链：PyTorch、HuggingFace、Weights & Biases 使用指南（内嵌代码示例）
- **论文写作指导：** 实验复现说明的写法，代码开源规范（README 模板）

#### 模块 8：前沿研究方向
- 图神经网络（GNN）：消息传递动画
- 对比学习（Contrastive Learning）：表示空间拉近/推远动画
- 强化学习基础：马尔可夫决策过程动画，Q-Learning 值函数更新
- 联邦学习（Federated Learning）：分布式训练节点通信动画
- **论文写作指导：** 综述论文（Survey）的结构规范，相关工作章节的写法

---

### 五、交互功能要求

- **全局搜索：** 支持关键词搜索知识点，结果高亮显示
- **学习进度追踪：** 侧边栏显示已浏览章节（localStorage 持久化），完成度进度条
- **代码沙盒：** 每个知识点配有可编辑的 Python 伪代码块，支持复制与语法高亮
- **公式悬浮卡片：** 鼠标悬停公式符号时，弹出该符号的含义说明
- **知识图谱页：** 用 D3.js Force Graph 展示各知识点间的依赖关系，点击节点跳转对应章节
- **夜间/日间模式切换**（CSS 变量实现，过渡动画 300ms）
- **章节内快速导航：** 文章右侧浮动目录，随滚动自动高亮当前章节

---

### 六、项目结构要求

```
ml-visualizer/
├── src/
│   ├── views/           # 各模块页面
│   ├── components/
│   │   ├── animations/  # 所有动画组件（神经网络、梯度下降等）
│   │   ├── charts/      # 图表组件
│   │   └── ui/          # 通用 UI 组件
│   ├── data/            # 知识点内容（JSON 或 Markdown）
│   ├── utils/           # 动画工具函数、数学计算
│   └── styles/          # 全局样式、CSS 变量
├── public/
├── index.html
└── vite.config.js
```

---

### 七、质量标准

- Lighthouse 性能评分 ≥ 85，动画使用 `requestAnimationFrame`，避免布局抖动
- 所有动画组件在组件卸载时必须清除定时器与 Canvas 渲染循环（防内存泄漏）
- 知识点内容必须准确，数学公式必须正确，引用论文必须真实存在（注明作者、年份、会议/期刊）
- 代码示例使用 PyTorch 2.x 语法，符合当前主流实践
- Edge 浏览器下所有动画、毛玻璃效果、CSS Grid 布局均需测试通过

---

**最终交付物：** 一个可在本地 `npm run dev` 直接运行的完整项目，包含完整的 `package.json` 依赖声明，以及一份 `README.md` 说明文档（含安装步骤、功能列表、技术选型说明）。

---

以上提示词涵盖了技术选型、视觉规范、动画细节、知识内容大纲、交互功能与质量标准六个维度，结构清晰，AI 按此执行时歧义极小。如需针对某个模块（例如仅生成 Transformer 可视化部分）单独拆分使用，可将"模块 5"单独提取作为独立提示词。